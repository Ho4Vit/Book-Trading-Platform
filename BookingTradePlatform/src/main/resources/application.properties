# App
spring.application.name=BookingTradePlatform
server.port=${SERVER_PORT:8080}
debug=${DEBUG:false}

spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:20MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:50MB}

# ===== DATABASE (Docker) =====
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:mysql}:3306/${MYSQL_DATABASE:BTP_DB}
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:1234}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.hikari.connection-timeout=${DB_CONN_TIMEOUT:60000}
spring.datasource.hikari.maximum-pool-size=${DB_MAX_POOL_SIZE:10}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=${HIBERNATE_DDL:update}
spring.jpa.show-sql=${HIBERNATE_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${HIBERNATE_FORMAT_SQL:true}
spring.jpa.properties.hibernate.dialect=${HIBERNATE_DIALECT:org.hibernate.dialect.MySQL8Dialect}

# ===== Redis (Docker) =====
spring.data.redis.host=${REDIS_HOST:redis}
spring.data.redis.port=${REDIS_PORT:6379}

# JWT
jwt.secret=${JWT_SECRET:u2Ce81T3asDzFZJyKu1zZrs4LKMoIMr7a24rdPFOfBM=}
jwt.expiration=${JWT_EXPIRATION:7200000}

# Mail
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:anhoa1794@gmail.com}
spring.mail.password=${MAIL_PASSWORD:xxxxxx}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_STARTTLS:true}

# Cloudinary
cloudinary.cloud-name=${CLOUDINARY_NAME:de8nzhr6n}
cloudinary.api-key=${CLOUDINARY_KEY:626523196151719}
cloudinary.api-secret=${CLOUDINARY_SECRET:fieJyWTHdWVCwMdT85YkVJRQyTo}

# Swagger
springdoc.api-docs.enabled=${SWAGGER_API_DOCS:true}
springdoc.swagger-ui.enabled=${SWAGGER_UI:true}
springdoc.swagger-ui.path=${SWAGGER_PATH:/swagger-ui.html}

# ===== Google OAuth2 =====
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.redirect-uri=${GOOGLE_REDIRECT_URI}
spring.security.oauth2.client.registration.google.scope=${GOOGLE_SCOPE:openid,profile,email}
spring.security.oauth2.client.provider.google.user-name-attribute=${GOOGLE_USERNAME_ATTR:sub}

# Gemini AI
spring.ai.vertex.ai.gemini.project-id=${GEMINI_PROJECT_ID}
spring.ai.vertex.ai.gemini.location=${GEMINI_LOCATION:asia-southeast1}
spring.ai.vertex.ai.gemini.credentials-uri=${GEMINI_CREDENTIALS:classpath:geminikey.json}
spring.ai.vertex.ai.gemini.transport=${GEMINI_TRANSPORT:rest}
spring.ai.vertex.ai.gemini.chat.options.model=${GEMINI_MODEL:gemini-2.0-flash}

# MoMo Sandbox
momo.partnerCode=${MOMO_PARTNER_CODE:MOMO}
momo.accessKey=${MOMO_ACCESS_KEY:F8BBA842ECF85}
momo.secretKey=${MOMO_SECRET_KEY:K951B6PE1waDMi640xX08PD3vg6EkVlz}
momo.endpoint=${MOMO_ENDPOINT:https://test-payment.momo.vn/v2/gateway/api/create}


vnpay.tmnCode=EEUM6OWW
vnpay.hashSecret=QXMH1GYF603HA0PK29MENO58U4BB6KPO
vnpay.payUrl=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
vnpay.returnUrl=http://localhost:8080/api/payments/vnpay-return